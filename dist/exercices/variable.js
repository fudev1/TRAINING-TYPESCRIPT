"use strict";
/**
 * Créer une variable :
 * myname qui n'accepte qu'un string
 * speed qui n'accepte que des nombres et est init à 25
 * isLoading qui n'accepte que les boolean
 * myage qui n'accepte les number ou les string
 * animals qui est un tableau de string et on effectue
 * un push de la valeur "otter".
 * info qui est un tableau contenant des strings ou des nombres ou des boolean
 * member qui ne peut être qu'un objet
 */
let myName;
let speed;
speed = 25;
// let speed: number = 25;
let isLoading;
let myAge;
// let animals: string[] = [];
// animals.push('otter');
let animals = ['otter'];
let info;
let member;
